This is a raytracer (technically a path tracer) built in rust. This path tracer is based on the Ray Tracing in One Weekend book.
The initial commit is everything from the book until (excluding) chapter 11.
